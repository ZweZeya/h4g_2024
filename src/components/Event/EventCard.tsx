import {useState} from "react";

const EventCard = ({
    title,
    text, 
    day,
    month,
}: {
    title: string, 
    text: string, 
    day: string,
    month: string
}) => {
  
    return (
        <div>
            <div className="cards">
            <div className="card">
            
            </div>
            </div>
            
        </div>
    );
};

export default EventCard